Binance Futures Scanner Rules Update - Smoke Test Report
Date: 2026-04-16

Checks performed:
1. Syntax validation passed for:
   - index.js
   - signals.js
   - dryrun.js
   - telegram.js
   - telegramMessageBuilder.js
   - state.js
   - config.js

2. Logic smoke tests passed for:
   - strict base timeframe filter (only 1m, 5m)
   - minimum 3 support timeframes including self
   - score > 80 filter for signal generation
   - adjusted Target 2 / SL2 generation from system TP1 and SL
   - fixed PNL1 model generation (+0.2% / -0.2%)
   - one blocking signal at a time
   - gate release after PNL1 or PNL2 closes
   - background monitoring continues for the other PNL model
   - separate PNL1 / PNL2 stats updates
   - full trade closure after both models settle

3. Smoke test outcome snapshot:
   - PNL1 target hit was detected correctly
   - new signal opened after first model closure
   - PNL2 target hit was detected correctly while previous monitoring completed
   - stats separated correctly for PNL1 and PNL2

Note:
- Runtime smoke test inside this container used a temporary local dotenv stub only because npm packages are not installed in this environment.
- The packaged project does not include that stub. Use `npm install` before running locally.
